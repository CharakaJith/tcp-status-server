# tcp-status-server
A multi-client TCP/IP server application that periodically polls connected clients for status, processes acknowledgements, and handles concurrent client communication with configurable polling control.
